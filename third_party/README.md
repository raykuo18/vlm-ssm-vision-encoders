# Third-Party Dependencies

This directory is reserved for pinned source dependencies used by the released inference backends.

Expected submodules:

- `VMamba`
- `MambaVision`
- `ViT-Adapter`
- `detectron2`

Each submodule keeps its upstream license files intact. See `NOTICE.md` for attribution notes.
